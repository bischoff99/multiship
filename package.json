{
  "name": "multiship",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "start": "pnpm --filter @app/api start",
    "db:push": "pnpm --filter @pkg/db prisma db push",
    "db:studio": "pnpm --filter @pkg/db prisma studio"
  }
}
